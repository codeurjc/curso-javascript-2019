{



    let names = ["Pedro", "Juan", "Sofía"];
    
    let first = names[0];
    
    for (let name of names) {
        console.log(name.length);
    }

}
{

    let numbers = [2, 43, 4, 5];
    
    let first = numbers[3];

    for (let num of numbers) {
        console.log(num / 2);
    }

    let bools: boolean[] = [];
    
    bools.push(true);
    bools.push("ss");

    let bool = bools.pop();

    
}